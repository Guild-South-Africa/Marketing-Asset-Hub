[build]
  command = "npm run build:netlify"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  BASE_PATH = "/"

# SPA fallback — Netlify serves /assets/* from dist first; this only catches app routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
